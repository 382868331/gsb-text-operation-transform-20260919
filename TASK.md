# 完整首轮任务

请为文本编辑器实现操作变换和组合。两份修改都可能基于同一段旧文本，合并时需要按下面的规则处理插入位置和删除范围。仓库里目前没有业务实现。

操作由Retain(n)、Delete(n)、Insert(text)组成，n按Unicode code point计数，拒绝负长度和非法UTF-8；零长度段移除，相邻同类段合并。必须恰好消耗源长度，组合字符不按字素簇处理。

提供Apply(source,op)、Invert(source,op)、Compose(a,b)。Compose等价先a后b，拒绝长度不匹配；不借助未知源文本构造组合，所有输入保持不变，长度算术检测溢出。

Transform(a,b,idA,idB)针对同源长度操作，返回aAfterB,bAfterA；ID为不同非空ASCII字符串，字典序较小者在同源位置先插入。删除只作用于原字符，不吞并发插入，重叠删除只生效一次。

保证Apply(Apply(s,a),bAfterA)=Apply(Apply(s,b),aAfterB)，逆操作恢复源，组合与两次Apply等价。仅两个并发操作的内核，不要求三操作TP2、网络会话、撤销栈或富文本。

输入和单操作总目标长度至多10000个code point。用固定种子短文本与合法操作生成器检查上述关系，含插入、删除交错；提供规范化结果及源/目标长度，演示同位置插入和重叠删除。

测试里请覆盖这些情况：负长度、同ID拒绝、同位插入、删除内部插入、完全/部分重叠删除、emoji/组合字符、空文本。

使用 Go 1.26.5 标准库，代码要能在 Windows 原生环境离线运行。范围就是这个库，不需要第三方依赖、外部服务、Docker 或前后端。请一并写好测试和简短的 README，说明接口及运行方法。

用 `go run ./cmd/demo` 运行演示，在约 8 秒内展示一个正常结果和一个实际触发的失败；用 `go test ./... -count=1 -timeout=60s` 运行测试。随机验证采用固定种子和少量小样本即可，不做长时间压力测试，也不扩展额外的平台功能。

请在本轮完成实现和验证，不要等我回复“继续”。正常排错需要的工具调用不设次数限制。操作范围限于当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区。不要添加 Actions、CI、部署或 Dependabot，也不要向远端推送。演示结果应由代码实际计算；最后如实说明验证结果，未运行或未通过的检查也请写明。
