# 完整首轮任务

纯文本编辑组件需要在本地合并两个并发编辑，并组合连续编辑。请从零实现“文本操作变换与组合内核”。

1. 操作由Retain(n)、Delete(n)、Insert(text)组成，n按Unicode code point计数，拒绝负长度和非法UTF-8；零长度段移除，相邻同类段合并。必须恰好消耗源长度，组合字符不按字素簇处理。
2. 提供Apply(source,op)、Invert(source,op)、Compose(a,b)。Compose等价先a后b，拒绝长度不匹配；不借助未知源文本构造组合，所有输入保持不变，长度算术检测溢出。
3. Transform(a,b,idA,idB)针对同源长度操作，返回aAfterB,bAfterA；ID为不同非空ASCII字符串，字典序较小者在同源位置先插入。删除只作用于原字符，不吞并发插入，重叠删除只生效一次。
4. 保证Apply(Apply(s,a),bAfterA)=Apply(Apply(s,b),aAfterB)，逆操作恢复源，组合与两次Apply等价。仅两个并发操作的内核，不要求三操作TP2、网络会话、撤销栈或富文本。
5. 输入和单操作总目标长度至多10000个code point。用固定种子短文本与合法操作生成器检查上述关系，含插入、删除交错；提供规范化结果及源/目标长度，演示同位置插入和重叠删除。

验收重点：负长度、同ID拒绝、同位插入、删除内部插入、完全/部分重叠删除、emoji/组合字符、空文本。

工程约束：全新0-1核心库，仅用Go 1.26.5标准库，在Windows原生离线运行，无第三方依赖、外部服务、Docker或前后端。交付实现、聚焦上述边界的测试及简短README；演示命令`go run ./cmd/demo`用小案例在约8秒内打印正常结果和一种真实失败，测试命令`go test ./... -count=1 -timeout=60s`。固定种子随机验证只用少量小样本，避免长压测和额外平台功能，不限制正常纠错所需工具调用。
一次首轮完成，不要求用户追加“继续”。只操作当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区；不添加Actions/CI/部署/Dependabot、不远程推送，不硬编码演示或虚报验证。
