# 完整首轮任务

请从零实现“文本操作变换与组合内核”，用于协作算法 / 操作变换中的本地可复用模块。

1. 从零实现文本操作库，操作由 Retain(n)、Delete(n)、Insert(text) 序列组成，n 按 Unicode code point 计数而非 UTF-8 字节；非法 UTF-8 拒绝。操作必须恰好消耗整个源文本，零长度段删除，相邻同类型段合并。
2. 提供 Apply、Invert(source,op) 与 Compose(a,b)，Compose 的结果等价于先 a 后 b，拒绝源/目标长度不匹配；实现流式片段合并，不能先构造未知源文本猜测操作。
3. Transform(a,b) 接受相同源长度的并发操作和两个不同、全序可比较的操作 ID，返回 aAfterB,bAfterA；同一源位置并发插入按 ID 升序，删除只删除原源字符，不吞掉另一侧插入，重叠删除只生效一次。
4. 公开规范化操作与长度元数据，所有方法不改写输入；用整数溢出检查拒绝非法长度。只做两操作变换内核，不实现网络、服务端会话、富文本或完整协作编辑器。
5. 测试 Apply(bAfterA,Apply(a,s))=Apply(aAfterB,Apply(b,s))、组合一致和逆操作恢复，用固定种子小文本与合法随机操作生成器验证；不要把随机结果写死。

验收重点：相同位置插入、删除区间内部插入、完全/部分重叠删除、emoji/组合字符、空文档、非法长度。

工程范围：这是全新核心库的0-1实现，初始仓库仅有运行骨架，没有业务实现。仅使用Go 1.26.5标准库，在Windows原生环境离线运行；无需安装第三方依赖。不接数据库、中间件、云API、外部程序，不使用Docker，不制作前后端或通用CLI。
交付：实现可复用库、聚焦上述语义的自动化测试和README；go run ./cmd/demo须在约8秒内运行确定性小案例，打印关键正常结果和至少一种真实边界失败，便于录制十几秒视频。测试命令为go test ./... -count=1 -timeout=60s。先阅读本仓库README和TASK.md，简短计划后集中实现，再运行针对性测试与演示；避免反复通读仓库、无关探索和无必要的长篇文档，发现实际问题时正常修复验证，不设硬性工具调用截断。
只能操作当前仓库内文件；不读取用户配置、密钥、环境变量清单或其他工作区。不要新增GitHub Actions、CI、部署、Dependabot或远程推送。一次首轮完成，不要求用户追加“继续”；合理实现取舍写入README。不要为通过测试删除需求、硬编码示例输出或宣称未实际执行的验证通过。
