# 文本操作变换与组合内核

当前为0-1任务的共同初始骨架，尚未实现业务功能。完整需求见 TASK.md。

## 环境与依赖

Windows原生 Go 1.26.5；仅标准库，无第三方安装步骤、外部服务、账号或密钥。

## 命令

演示：`go run ./cmd/demo`

测试：`go test ./... -count=1 -timeout=60s`

初始演示仅明确打印骨架提示，初始无业务测试；命令可启动不代表题目实现或验收通过。模型须补充真实实现、测试及短演示。

## 范围

协作算法 / 操作变换。同位置插入顺序、删除重叠、Unicode 索引和操作组合需满足可检验的代数关系。

不设置工具调用次数上限，不执行网络或后台任务。禁止GitHub自动执行；不提交凭据、配置或运行轨迹。
